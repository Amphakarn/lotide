const letterPositions = function(sentence) {
  const results = {};
  // logic to update results here
  return results;
};

const findCoffee = function(beans) {
  beans.forEach(grind);
};
console.log(findCoffee('java'));